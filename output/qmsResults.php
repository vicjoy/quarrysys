<?php

	//include the file here now....
	include "result_handler.php";

	if ($db_found) {

		$SQL = $db_found->prepare('SELECT * FROM qms_table WHERE ID = 1');
		if ($SQL) {
			//$SQL->bind_param('i', $qNum);
			$SQL->execute();

			$result = $SQL->get_result();


						include "chart.php";

									}
						else {
							print "ROW ERROR";
						}
					}else {
					print "database error";
				}
		if ($db_found) {

				$SQL = $db_found->prepare('SELECT * FROM qms_table WHERE ID = 2');
				if ($SQL) {
					//$SQL->bind_param('i', $qNum);
					$SQL->execute();

					$result = $SQL->get_result();


						include "chart.php";

									}

						else {
							print "ROW ERROR";
						}
					}else {
					print "database error";
				}
		if ($db_found) {

				$SQL = $db_found->prepare('SELECT * FROM qms_table WHERE ID = 3');
				if ($SQL) {
					//$SQL->bind_param('i', $qNum);
					$SQL->execute();

					$result = $SQL->get_result();


						include "chart.php";

									}

						else {
							print "ROW ERROR";
						}
					}else {
					print "database error";
				}
		if ($db_found) {

				$SQL = $db_found->prepare('SELECT * FROM qms_table WHERE ID = 4');
				if ($SQL) {
					//$SQL->bind_param('i', $qNum);
					$SQL->execute();

					$result = $SQL->get_result();


						include "chart.php";

									}

						else {
							print "ROW ERROR";
						}
					}else {
					print "database error";
				}
		if ($db_found) {

				$SQL = $db_found->prepare('SELECT * FROM qms_table WHERE ID = 5');
				if ($SQL) {
					//$SQL->bind_param('i', $qNum);
					$SQL->execute();

					$result = $SQL->get_result();


						include "chart.php";

									}

						else {
							print "ROW ERROR";
						}
					}else {
					print "database error";
				}
		if ($db_found) {

				$SQL = $db_found->prepare('SELECT * FROM qms_table WHERE ID = 6');
				if ($SQL) {
					//$SQL->bind_param('i', $qNum);
					$SQL->execute();

					$result = $SQL->get_result();


						include "chart.php";

									}

						else {
							print "ROW ERROR";
						}
					}else {
					print "database error";
				}
		if ($db_found) {

				$SQL = $db_found->prepare('SELECT * FROM qms_table WHERE ID = 7');
				if ($SQL) {
					//$SQL->bind_param('i', $qNum);
					$SQL->execute();

					$result = $SQL->get_result();


						include "chart.php";

									}

						else {
							print "ROW ERROR";
						}
					}else {
					print "database error";
				}
		if ($db_found) {

				$SQL = $db_found->prepare('SELECT * FROM qms_table WHERE ID = 8');
				if ($SQL) {
					//$SQL->bind_param('i', $qNum);
					$SQL->execute();

					$result = $SQL->get_result();


						include "chart.php";

									}

						else {
							print "ROW ERROR";
						}
					}else {
					print "database error";
				}
		if ($db_found) {

				$SQL = $db_found->prepare('SELECT * FROM qms_table WHERE ID = 9');
				if ($SQL) {
					//$SQL->bind_param('i', $qNum);
					$SQL->execute();

					$result = $SQL->get_result();


						include "chart.php";

									}

						else {
							print "ROW ERROR";
						}
					}else {
					print "database error";
				}
		if ($db_found) {

				$SQL = $db_found->prepare('SELECT * FROM qms_table WHERE ID = 10');
				if ($SQL) {
					//$SQL->bind_param('i', $qNum);
					$SQL->execute();

					$result = $SQL->get_result();


						include "chart.php";

									}

						else {
							print "ROW ERROR";
						}
					}else {
					print "database error";
				}
		if ($db_found) {

				$SQL = $db_found->prepare('SELECT * FROM qms_table WHERE ID = 11');
				if ($SQL) {
					//$SQL->bind_param('i', $qNum);
					$SQL->execute();

					$result = $SQL->get_result();


						include "chart.php";

									}

						else {
							print "ROW ERROR";
						}
					}else {
					print "database error";
				}
		if ($db_found) {

				$SQL = $db_found->prepare('SELECT * FROM qms_table WHERE ID = 12');
				if ($SQL) {
					//$SQL->bind_param('i', $qNum);
					$SQL->execute();

					$result = $SQL->get_result();


						include "chart.php";

									}

						else {
							print "ROW ERROR";
						}
					}else {
					print "database error";
				}
		if ($db_found) {

				$SQL = $db_found->prepare('SELECT * FROM qms_table WHERE ID = 13');
				if ($SQL) {
					//$SQL->bind_param('i', $qNum);
					$SQL->execute();

					$result = $SQL->get_result();


						include "chart.php";

									}

						else {
							print "ROW ERROR";
						}
					}else {
					print "database error";
				}
		if ($db_found) {

				$SQL = $db_found->prepare('SELECT * FROM qms_table WHERE ID = 14');
				if ($SQL) {
					//$SQL->bind_param('i', $qNum);
					$SQL->execute();

					$result = $SQL->get_result();


						include "chart.php";

									}

						else {
							print "ROW ERROR";
						}
					}else {
					print "database error";
				}
		if ($db_found) {

				$SQL = $db_found->prepare('SELECT * FROM qms_table WHERE ID = 15');
				if ($SQL) {
					//$SQL->bind_param('i', $qNum);
					$SQL->execute();

					$result = $SQL->get_result();


						include "chart.php";

									}

						else {
							print "ROW ERROR";
						}
					}else {
					print "database error";
				}
		if ($db_found) {

				$SQL = $db_found->prepare('SELECT * FROM qms_table WHERE ID = 16');
				if ($SQL) {
					//$SQL->bind_param('i', $qNum);
					$SQL->execute();

					$result = $SQL->get_result();


						include "chart.php";

									}

						else {
							print "ROW ERROR";
						}
					}else {
					print "database error";
				}
		if ($db_found) {

				$SQL = $db_found->prepare('SELECT * FROM qms_table WHERE ID = 17');
				if ($SQL) {
					//$SQL->bind_param('i', $qNum);
					$SQL->execute();

					$result = $SQL->get_result();


						include "chart.php";

									}

						else {
							print "ROW ERROR";
						}
					}else {
					print "database error";
				}
		if ($db_found) {

				$SQL = $db_found->prepare('SELECT * FROM qms_table WHERE ID = 18');
				if ($SQL) {
					//$SQL->bind_param('i', $qNum);
					$SQL->execute();

					$result = $SQL->get_result();


						include "chart.php";

									}

						else {
							print "ROW ERROR";
						}
					}else {
					print "database error";
				}
		





/*
}
else {
	print "no results to display";
}
*/
?>

